services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: warren
      POSTGRES_PASSWORD: warren
      POSTGRES_DB: warren
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
